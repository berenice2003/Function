import UIKit


var str = "Hello, Everybody"

func greet(person:String) -> String {
    let greeting = "Hello, " + person + "!"
    return greeting
    }

print(greet(person: "Berenice"))

    var a = 10
    var b = 40

func plus(min: Int, max: Int) -> Int {
    let a = 10
    let b = 40
    return a + b
}
print(plus(min: a, max: b))
